# @weather-lsa/shared

Schemas and shared types for the Next.js + FastAPI monorepo.

- Zod schemas for map state and related shared contracts.
- Built with tsup; outputs ESM and CJS with type declarations.

Build:

- pnpm -w --filter @weather-lsa/shared build
